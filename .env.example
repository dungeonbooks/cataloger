# Hardcover API token (get one at https://hardcover.app/account/api)
HARDCOVER_TOKEN="Bearer your-token-here"

# Environment: "dev" enables hot reload, anything else is production
# ENV=prod

# Rate limiting (optional)
# RATE_LIMIT=10
# RATE_LIMIT_WINDOW=60

# Cache (optional)
# CACHE_DIR=.cache
# CACHE_TTL_DAYS=7
